# Public 版では workspace_dir は engine からは参照されない。
# BEATEMUP_RUNTIME_DIR 環境変数 (justfile の engine-run-sample が設定) で
# sample-projects/minimal を渡す。
#
# 注意: 旧 `window: {width, height}` セクションは ADR-0041 で Bevy App Settings に
# 移管したためここからは外れた。初回起動時の解像度は内部 fallback
# (`WINDOW_INTEGER_SCALE_FALLBACK = 3` × viewport) で決まる。
